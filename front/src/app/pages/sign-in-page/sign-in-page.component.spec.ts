import { SignInPageComponent } from './sign-in-page.component';

describe('SignInPageComponent', () => {
  let component: SignInPageComponent;

  beforeEach(async () => {
    component = new SignInPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
