import { SignUpPageComponent } from './sign-up-page.component';

describe('SignUpPageComponent', () => {
  let component: SignUpPageComponent;

  beforeEach(async () => {
    component = new SignUpPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
