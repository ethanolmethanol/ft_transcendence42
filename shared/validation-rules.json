{
  "username": {
    "required": true,
    "minLength": 3,
    "maxLength": 20,
    "pattern": "^[a-zA-Z0-9@.+-_]+$"
  },
  "email": {
    "required": true,
    "pattern": "^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$"
  },
  "password": {
    "required": true,
    "minLength": 8
  }
}
